import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Audio service requires platform channels — skipped in unit tests
  });
}
